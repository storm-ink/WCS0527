# AGENTS.md

## Cursor Cloud specific instructions

This repository (`WCS0527`) is currently an empty/freshly initialized project with no source code, dependencies, or services. The only file is `README.md`.

### Current State

- No programming language or framework has been chosen yet.
- No package manager, build system, or test framework is configured.
- No services need to be started.
- No linting, testing, or build commands are available.

### When Code Is Added

Once the repository gains source code and dependencies, this section should be updated with:

- How to install dependencies (update script details).
- How to run lint, tests, and build commands.
- How to start development servers or services.
- Any non-obvious caveats discovered during setup.
