using System;
using System.Collections;
using System.Collections.Generic;
using System.Drawing.Design;
using System.Linq;
using System.Reflection;
using System.Text;

namespace Wcs.Client.App.Elements.UIEditor
{
    public class CarsEditor : UITypeEditor
    {
        public override object EditValue(System.ComponentModel.ITypeDescriptorContext context, IServiceProvider provider, object value)
        {
            System.Collections.IList list = (System.Collections.IList)value; 

            var xx = list.Cast<ShuttleCar>() ;
            var shuttle = (Shuttle)context.Instance;

            using (frmCarsEditorUI frm = new frmCarsEditorUI(xx.ToList(), shuttle))
            {
                if (frm.ShowDialog() == System.Windows.Forms.DialogResult.OK)
                {
                    value = frm.Cars.ToArray();
                }
            }

            return value;
        }

        public override UITypeEditorEditStyle GetEditStyle(System.ComponentModel.ITypeDescriptorContext context)
        {
            return UITypeEditorEditStyle.Modal;
        }


    }
}
